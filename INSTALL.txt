FANTASY MPL — LIVE DRAFT UI REBUILD

1. Copy the CONTENTS of this folder into the repository root.
2. Do not copy the outer fantasy-mpl-live-draft-ui-rebuild folder.
3. Replace matching files.
4. No Supabase migration is required.
5. Commit message:
   Rebuild Live Draft hero picker and guidance UI

VERIFY ON MOBILE
- Opening a ban/pick slot does not focus the search field automatically.
- The keyboard stays closed until the user taps Search.
- Search uses 16px text and does not trigger browser zoom.
- The current action and guidance status appear before the hero grid.
- Hero cards use two readable columns.
- The turn bar no longer overlaps the side header while scrolling.

VERIFY ON DESKTOP
- Hero picker uses a six-column catalog and a dedicated guidance rail.
- Guidance is visible while choosing a hero.
- Priority Ban, Potential Pick and Counter Pick become clickable when verified data is available.
- Public header has dark high-contrast styling.
- Hero banner uses the compact Fantasy MPL Draft Intelligence identity card.
