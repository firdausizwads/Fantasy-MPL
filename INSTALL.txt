FANTASY MPL — PROFILE NETWORK HOTFIX

1. Copy the CONTENTS of this folder into the repository root.
2. Do not copy the outer fantasy-mpl-profile-network-hotfix folder.
3. Replace app/page.tsx.
4. No new Supabase migration is required if Migration 023 already returned:
   atomic_profile_save_ready: true
   account_bootstrap_ready: true
5. Commit message:
   Fix mobile avatar upload and transient account loading

FIXES
- Converts canvas data URLs directly to binary instead of fetch(data:...), avoiding mobile/Safari Load failed errors.
- Retries one transient avatar upload failure.
- Retries one transient atomic profile-save failure.
- Retries session and account bootstrap once before showing recovery.
- Ignores duplicate INITIAL_SESSION/TOKEN_REFRESHED hydration events.
- Shows a clearer network error only after both attempts fail.
