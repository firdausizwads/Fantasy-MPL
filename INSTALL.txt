FANTASY MPL — PANDASCORE LIST NORMALIZATION HOTFIX

1. Copy the CONTENTS of this folder into the repository root.
2. Do not copy the outer fantasy-mpl-pandascore-normalization-hotfix folder.
3. Replace matching files.
4. No Supabase migration is required.
5. Commit:
   Fix PandaScore Season 18 list normalization
6. Push main and wait for Vercel Production to become Ready.

CAUSE
PandaScore single-match responses include serie.season and serie.year, but list responses may expose only serie.full_name. The connector required numeric season/year, so valid Season 18 2026 records could all be rejected before staging.

FIX
- Accepts verified "Season 18 2026" labels when numeric fields are omitted.
- Retrieves missing series start dates from /series/{id}.
- Separates raw provider count from accepted/staged count.
- Shows rejection categories: wrongSeason, opponents, missingTime, missingSeriesStart.

AFTER DEPLOYMENT
1. Admin Console > Fixture sync.
2. Click SYNC NOW once.
3. Send the persistent result line if accepted/staged is still zero.
