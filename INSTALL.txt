FANTASY MPL — FIXTURE SYNC DIAGNOSTICS & REGION PERSISTENCE HOTFIX

1. Copy the CONTENTS of this folder into the repository root.
2. Do not copy the outer fantasy-mpl-fixture-sync-region-hotfix folder.
3. Replace matching files.
4. No Supabase migration is required; Migration 024 remains valid.
5. Commit:
   Fix regional tab persistence and PandaScore sync diagnostics
6. Push main and wait for Vercel Production to become Ready.

REGION FIX
- Current in-memory region now has first priority during auth refresh.
- Current /my, /id or /ph pathname is used before the original mounted route.
- Returning to a background tab no longer resets ID/PH to MY.

SYNC DIAGNOSTICS
- SYNC NOW now displays a persistent success or error panel.
- Success shows fetched, staged and unresolved counts.
- Errors show HTTP status and the exact safe server/database message.
- Missing environment variable names are identified without exposing values.

AFTER DEPLOYMENT
1. Open Admin Console > Fixture sync.
2. Click SYNC NOW once.
3. If successful, review mappings.
4. If failed, send the persistent error text shown on screen; never send secrets.
