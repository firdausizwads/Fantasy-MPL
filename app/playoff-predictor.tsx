'use client';
import { useEffect, useState } from 'react';
import type { ComponentType, ReactNode } from 'react';
import { supabase } from '../lib/supabase/client';
type Region='MY'|'ID'|'PH';
export type PlayoffTeam={code:string;name:string;logo:string};
type PageBannerProps={tag:string;title:string;copy:string;side:ReactNode;sideLabel:string};
const REGION_INFO:Record<Region,{name:string;logo:string}>={MY:{name:'MPL Malaysia',logo:'/leagues/mpl-my.png'},ID:{name:'MPL Indonesia',logo:'/leagues/mpl-id.png'},PH:{name:'MPL Philippines',logo:'/leagues/mpl-ph.png'}};
function roundRect(ctx:CanvasRenderingContext2D,x:number,y:number,w:number,h:number,r:number){ctx.beginPath();ctx.roundRect(x,y,w,h,r)}
function drawImageContain(ctx:CanvasRenderingContext2D,image:HTMLImageElement,x:number,y:number,w:number,h:number){const ratio=Math.min(w/image.width,h/image.height),dw=image.width*ratio,dh=image.height*ratio;ctx.drawImage(image,x+(w-dw)/2,y+(h-dh)/2,dw,dh)}
type BracketPick={winner?:string;score?:string};
const PLAYOFF_MATCHES=[
 {id:'P1',label:'PLAY-IN 1',round:'PLAY-INS',bestOf:5},{id:'P2',label:'PLAY-IN 2',round:'PLAY-INS',bestOf:5},
 {id:'U1',label:'UPPER SEMIFINAL 1',round:'UPPER SEMIS',bestOf:5},{id:'U2',label:'UPPER SEMIFINAL 2',round:'UPPER SEMIS',bestOf:5},
 {id:'UF',label:'UPPER FINAL',round:'FINALS PATH',bestOf:5},{id:'LS',label:'LOWER SEMIFINAL',round:'FINALS PATH',bestOf:5},
 {id:'LF',label:'LOWER FINAL',round:'LOWER FINAL',bestOf:7},{id:'GF',label:'GRAND FINAL',round:'GRAND FINAL',bestOf:7}
] as const;
export default function PlayoffPredictor({region,notify,teams,teamIndex,PageBanner}:{region:Region;notify:(s:string)=>void;teams:PlayoffTeam[];teamIndex:Record<string,{name:string;logo:string}>;PageBanner:ComponentType<PageBannerProps>}){const[mode,setMode]=useState<'custom'|'official'>('custom');const[customSeeds,setCustomSeeds]=useState<string[]>(teams.slice(0,6).map(team=>team.code));const[playoffStart,setPlayoffStart]=useState('');const officialOpen=Boolean(playoffStart)&&Date.now()>=new Date(playoffStart).getTime()-7*86400000;const bracketTeams=(mode==='custom'?customSeeds.map(code=>teams.find(team=>team.code===code)).filter((team):team is PlayoffTeam=>Boolean(team)):teams.slice(0,6));const seeds:Record<number,string>=Object.fromEntries(bracketTeams.map((t,i)=>[i+1,t.code]));const storage=`fmpl_playoffs_${region}_${mode}`;const [picks,setPicks]=useState<Record<string,BracketPick>>({});const [submitted,setSubmitted]=useState(false);useEffect(()=>{setCustomSeeds(teams.slice(0,6).map(team=>team.code))},[region,teams]);useEffect(()=>{try{const raw=localStorage.getItem(storage);if(raw){const value=JSON.parse(raw);setPicks(value.picks||{});setSubmitted(Boolean(value.submitted))}else{setPicks({});setSubmitted(false)}}catch{}},[region,mode]);useEffect(()=>{let active=true;(async()=>{if(!supabase)return;const{data:season}=await supabase.from('seasons').select('id').eq('region_code',region).eq('season_number',18).maybeSingle();if(!season)return;const{data:match}=await supabase.from('matches').select('scheduled_at').eq('season_id',season.id).in('best_of',[5,7]).neq('status','cancelled').order('scheduled_at').limit(1).maybeSingle();if(active)setPlayoffStart(match?.scheduled_at||'')})();return()=>{active=false}},[region]);const winner=(id:string)=>picks[id]?.winner;const participants=(id:string):Array<string|undefined>=>{const loser=(match:string)=>{const pair=participants(match),win=winner(match);return win?pair.find(x=>x&&x!==win):undefined};switch(id){case'P1':return[seeds[3],seeds[6]];case'P2':return[seeds[4],seeds[5]];case'U1':return[seeds[1],winner('P2')];case'U2':return[seeds[2],winner('P1')];case'UF':return[winner('U1'),winner('U2')];case'LS':return[loser('U1'),loser('U2')];case'LF':return[loser('UF'),winner('LS')];case'GF':return[winner('UF'),winner('LF')];default:return[]}};function choose(id:string,team:string){if(submitted)return;const index=PLAYOFF_MATCHES.findIndex(m=>m.id===id);const next={...picks,[id]:{winner:team,score:picks[id]?.winner===team?picks[id]?.score:undefined}};PLAYOFF_MATCHES.slice(index+1).forEach(m=>delete next[m.id]);setPicks(next)}function score(id:string,value:string){if(submitted)return;setPicks({...picks,[id]:{...picks[id],score:value}})}function save(lock=false){localStorage.setItem(storage,JSON.stringify({picks,submitted:lock}));setSubmitted(lock);notify(lock?'Playoff bracket locked for this preview.':'Playoff bracket saved.')}function changeSeed(index:number,code:string){setCustomSeeds(current=>{const next=[...current];const other=next.indexOf(code);if(other>=0)[next[index],next[other]]=[next[other],next[index]];else next[index]=code;return next});setPicks({});setSubmitted(false)}const complete=PLAYOFF_MATCHES.every(m=>picks[m.id]?.winner&&picks[m.id]?.score);const champion=winner('GF');const champ=bracketTeams.find(t=>t.code===champion);async function exportBracket(){const canvas=document.createElement('canvas');canvas.width=1920;canvas.height=1080;const ctx=canvas.getContext('2d');if(!ctx)return;const load=(src:string)=>new Promise<HTMLImageElement|null>(resolve=>{const image=new Image();image.onload=()=>resolve(image);image.onerror=()=>resolve(null);image.src=src});const [leagueLogo,brandLogo]=await Promise.all([load(REGION_INFO[region].logo),load('/brand/fantasy-mpl-emblem.png')]);const loadedTeamLogos=await Promise.all(bracketTeams.map(team=>load(team.logo)));const teamLogoMap:Record<string,HTMLImageElement>={};bracketTeams.forEach((team,index)=>{const image=loadedTeamLogos[index];if(image)teamLogoMap[team.code]=image});const colors:Record<Region,[string,string,string]>={MY:['#061a3c','#1257aa','#f2c633'],ID:['#240b13','#9e2031','#ff6979'],PH:['#081a36','#175fb7','#f3b826']};const [dark,mid,accent]=colors[region];const gradient=ctx.createLinearGradient(0,0,1920,1080);gradient.addColorStop(0,dark);gradient.addColorStop(.62,mid);gradient.addColorStop(1,dark);ctx.fillStyle=gradient;ctx.fillRect(0,0,1920,1080);ctx.globalAlpha=.08;ctx.strokeStyle='#fff';ctx.lineWidth=1;for(let x=-500;x<2200;x+=90){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x+700,1080);ctx.stroke()}ctx.globalAlpha=1;const glow=ctx.createRadialGradient(1500,200,10,1500,200,520);glow.addColorStop(0,accent+'55');glow.addColorStop(1,'transparent');ctx.fillStyle=glow;ctx.fillRect(900,0,1020,800);if(brandLogo)ctx.drawImage(brandLogo,66,48,75,75);ctx.fillStyle='#fff';ctx.font='800 44px Arial';ctx.fillText('FANTASY MPL',155,84);ctx.fillStyle='rgba(255,255,255,.6)';ctx.font='700 16px Arial';ctx.fillText('PLAYOFF BRACKET PREDICTION',157,112);if(leagueLogo){const ratio=leagueLogo.width/leagueLogo.height;const h=105,w=h*ratio;ctx.drawImage(leagueLogo,1810-w,35,w,h)}ctx.textAlign='right';ctx.fillStyle=accent;ctx.font='800 19px Arial';ctx.fillText(REGION_INFO[region].name.toUpperCase(),1805,163);ctx.textAlign='left';ctx.fillStyle='#fff';ctx.font='800 32px Arial';ctx.fillText('ROAD TO THE GRAND FINAL',66,193);ctx.fillStyle='rgba(255,255,255,.58)';ctx.font='600 14px Arial';ctx.fillText('SEASON 18 · FULL BRACKET · 1920 × 1080',68,220);const stages=[{title:'01 · PLAY-IN',ids:['P1','P2']},{title:'02 · UPPER & LOWER BRACKET',ids:['U1','U2','LS']},{title:'03 · UPPER & LOWER FINALS',ids:['UF','LF']},{title:'04 · GRAND FINAL',ids:['GF']}];const colW=425,gap=25,startX=65;stages.forEach((stage,col)=>{const x=startX+col*(colW+gap);ctx.fillStyle='rgba(255,255,255,.08)';roundRect(ctx,x,252,colW,690,16);ctx.fill();ctx.strokeStyle='rgba(255,255,255,.13)';ctx.stroke();ctx.fillStyle=accent;ctx.font='800 16px Arial';ctx.fillText(stage.title,x+20,287);ctx.fillStyle='rgba(255,255,255,.43)';ctx.font='600 11px Arial';ctx.fillText(col===0?'SEEDS 3–6 · ELIMINATION':col===1?'DOUBLE-ELIMINATION PATH':col===2?'WINNERS REACH THE TITLE MATCH':'CHAMPIONSHIP SERIES',x+20,308);const availableH=590;const cardH=stage.ids.length===3?175:stage.ids.length===2?225:275;const cardGap=(availableH-stage.ids.length*cardH)/(stage.ids.length+1);stage.ids.forEach((id,row)=>{const y=322+cardGap*(row+1)+cardH*row;ctx.fillStyle='rgba(5,12,22,.56)';roundRect(ctx,x+18,y,colW-36,cardH,11);ctx.fill();ctx.strokeStyle='rgba(255,255,255,.14)';ctx.stroke();const def=PLAYOFF_MATCHES.find(m=>m.id===id)!;ctx.fillStyle=accent;ctx.font='800 11px Arial';ctx.fillText(`${def.label} · BO${def.bestOf}`,x+35,y+27);const pair=participants(id);pair.forEach((code,i)=>{const team=code?teamIndex[code]:undefined;const rowY=y+48+i*49;ctx.fillStyle=picks[id]?.winner===code?'rgba(255,255,255,.17)':'rgba(255,255,255,.065)';roundRect(ctx,x+31,rowY,colW-62,39,7);ctx.fill();ctx.fillStyle='#fff';ctx.font='700 12px Arial';if(code&&teamLogoMap[code])drawImageContain(ctx,teamLogoMap[code],x+42,rowY+5,29,29);ctx.fillText(team?.name||'TO BE DECIDED',x+(team?82:44),rowY+25);if(picks[id]?.winner===code){ctx.fillStyle=accent;ctx.font='800 13px Arial';ctx.fillText('✓',x+colW-52,rowY+25)}});ctx.strokeStyle='rgba(255,255,255,.11)';ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(x+31,y+cardH-39);ctx.lineTo(x+colW-31,y+cardH-39);ctx.stroke();ctx.fillStyle='rgba(255,255,255,.035)';roundRect(ctx,x+31,y+cardH-33,colW-62,24,5);ctx.fill();ctx.fillStyle='rgba(255,255,255,.7)';ctx.font='700 10px Arial';ctx.fillText(`EXACT SCORE`,x+43,y+cardH-16);ctx.textAlign='right';ctx.fillStyle=accent;ctx.font='800 12px Arial';ctx.fillText(picks[id]?.score||'—',x+colW-43,y+cardH-16);ctx.textAlign='left'});if(col<3){ctx.strokeStyle=accent;ctx.lineWidth=2;ctx.globalAlpha=.7;ctx.beginPath();ctx.moveTo(x+colW,595);ctx.lineTo(x+colW+gap,595);ctx.stroke();ctx.globalAlpha=1}});ctx.fillStyle='rgba(5,12,22,.78)';roundRect(ctx,1375,950,480,94,12);ctx.fill();ctx.strokeStyle=accent;ctx.stroke();if(champ&&teamLogoMap[champ.code])drawImageContain(ctx,teamLogoMap[champ.code],1395,965,58,58);ctx.fillStyle=accent;ctx.font='800 11px Arial';ctx.fillText(`PREDICTED ${REGION_INFO[region].name.toUpperCase()} CHAMPION`,1470,981);ctx.fillStyle='#fff';ctx.font='800 24px Arial';ctx.fillText(champ?.name||'NOT SELECTED',1470,1017);ctx.fillStyle='rgba(255,255,255,.48)';ctx.font='600 11px Arial';ctx.fillText('CREATED WITH FANTASY MPL',67,1020);canvas.toBlob(blob=>{if(!blob)return;const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`fantasy-mpl-${region.toLowerCase()}-playoff-bracket.png`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)},'image/png')}const modeNav=<div className="playoffModeTabs"><button className={mode==='custom'?'active':''} onClick={()=>setMode('custom')}><b>CUSTOM BRACKET</b><small>AVAILABLE ANYTIME</small></button><button className={mode==='official'?'active':''} disabled={!officialOpen} onClick={()=>officialOpen&&setMode('official')}><b>OFFICIAL PREDICTOR</b><small>{officialOpen?'OPEN NOW':playoffStart?`OPENS ${new Date(new Date(playoffStart).getTime()-7*86400000).toLocaleDateString()}`:'PLAYOFF DATE PENDING'}</small></button></div>;if(mode==='official'&&!officialOpen)return <div className="page playoffPage playoffV2"><PageBanner tag={`${REGION_INFO[region].name.toUpperCase()} · OFFICIAL PLAYOFFS`} title="Official Predictor Opens Soon" copy="The official bracket becomes available seven days before the first imported playoff match." side={playoffStart?new Date(playoffStart).toLocaleDateString():'—'} sideLabel="PLAYOFF START"/>{modeNav}<section className="officialPlayoffGate"><img src={REGION_INFO[region].logo} alt=""/><span>OFFICIAL BRACKET LOCKED</span><h2>Use Custom Bracket now.</h2><p>Create, edit and export your own six-team bracket while official seeding and fixtures are still pending.</p><button onClick={()=>setMode('custom')}>OPEN CUSTOM BRACKET</button></section></div>;return <div className="page playoffPage playoffV2"><PageBanner tag={`${REGION_INFO[region].name.toUpperCase()} · PLAYOFF CHALLENGE`} title="Road to the Grand Final" copy="Predict every winner and exact series score through the complete regional bracket." side={submitted?'LOCKED':`${Object.keys(picks).length} / 8`} sideLabel={submitted?'BRACKET STATUS':'SERIES PREDICTED'}/>{modeNav}{mode==='custom'&&<section className="customSeedPanel"><header><div><span>CUSTOM SANDBOX</span><h2>Choose your six seeds</h2><p>Swap any regional teams and build a bracket before official playoffs open.</p></div></header><div>{customSeeds.map((code,index)=><label key={index}><small>SEED {index+1}</small><select value={code} onChange={event=>changeSeed(index,event.target.value)}>{teams.map(team=><option value={team.code} key={team.code}>{team.name}</option>)}</select></label>)}</div></section>}<div className="playoffPreview"><span>PREVIEW SEEDING</span><p>OFFICIAL PLAYOFF SEEDS WILL REPLACE THESE DEMONSTRATION POSITIONS AFTER THE REGULAR SEASON.</p><div className="bracketShare"><b>SHAREABLE BRACKET CARD</b><button onClick={exportBracket}><span>↓</span><div><strong>EXPORT BRACKET CARD</strong><small>PNG · 1920 × 1080</small></div></button></div></div><div className="playoffScoreRules"><div><strong>+40</strong><span>SERIES WINNER</span></div><div><strong>+25</strong><span>EXACT SCORE</span></div><div><strong>+75</strong><span>EACH FINALIST</span></div><div><strong>+150</strong><span>CHAMPION</span></div><div><strong>+250</strong><span>PERFECT BRACKET</span></div></div><div className="bracketScroll"><div className="bracketGrid playoffBracketV2"><BracketColumn title="01 · PLAY-IN" subtitle="SEEDS 3–6 · ELIMINATION"><BracketMatch teamIndex={teamIndex} match={PLAYOFF_MATCHES[0]} teams={participants('P1')} picks={picks} choose={choose} score={score} locked={submitted}/><BracketMatch teamIndex={teamIndex} match={PLAYOFF_MATCHES[1]} teams={participants('P2')} picks={picks} choose={choose} score={score} locked={submitted}/></BracketColumn><BracketColumn title="02 · UPPER & LOWER BRACKET" subtitle="DOUBLE-ELIMINATION PATH"><BracketMatch teamIndex={teamIndex} match={PLAYOFF_MATCHES[2]} teams={participants('U1')} picks={picks} choose={choose} score={score} locked={submitted}/><BracketMatch teamIndex={teamIndex} match={PLAYOFF_MATCHES[3]} teams={participants('U2')} picks={picks} choose={choose} score={score} locked={submitted}/><BracketMatch teamIndex={teamIndex} match={PLAYOFF_MATCHES[5]} teams={participants('LS')} picks={picks} choose={choose} score={score} locked={submitted}/></BracketColumn><BracketColumn title="03 · UPPER & LOWER FINALS" subtitle="WINNERS REACH THE TITLE MATCH"><BracketMatch teamIndex={teamIndex} match={PLAYOFF_MATCHES[4]} teams={participants('UF')} picks={picks} choose={choose} score={score} locked={submitted}/><BracketMatch teamIndex={teamIndex} match={PLAYOFF_MATCHES[6]} teams={participants('LF')} picks={picks} choose={choose} score={score} locked={submitted}/></BracketColumn><BracketColumn title="04 · GRAND FINAL" subtitle="BEST OF 7 · CHAMPIONSHIP"><BracketMatch teamIndex={teamIndex} match={PLAYOFF_MATCHES[7]} teams={participants('GF')} picks={picks} choose={choose} score={score} locked={submitted}/>{champ&&<div className="predictedChampion"><span>PREDICTED {REGION_INFO[region].name.toUpperCase()} CHAMPION</span><img src={champ.logo} alt={`${champ.name} logo`}/><strong>{champ.name}</strong></div>}</BracketColumn></div></div><div className="bracketFooter"><div><span className={complete?'complete':'incomplete'}>{complete?'✓':'!'}</span><p><b>{complete?'BRACKET COMPLETE':'PREDICTIONS INCOMPLETE'}</b><small>{complete?'EVERY SERIES HAS A WINNER AND SCORE.':'COMPLETE ALL EIGHT SERIES AND SCORES BEFORE LOCKING.'}</small></p></div><div><button className="secondary" disabled={submitted} onClick={()=>save(false)}>SAVE DRAFT</button>{submitted?<button className="secondary" onClick={()=>{setSubmitted(false);localStorage.setItem(storage,JSON.stringify({picks,submitted:false}));notify('Preview bracket unlocked.')}}>UNLOCK PREVIEW</button>:<button className="primary" disabled={!complete} onClick={()=>save(true)}>LOCK COMPLETE BRACKET</button>}</div></div></div>}
function BracketColumn({title,subtitle,children}:{title:string;subtitle:string;children:ReactNode}){return <section className="bracketColumn"><div className="bracketColumnHead"><h2>{title}</h2><p>{subtitle}</p></div><div className="bracketMatches">{children}</div></section>}
function BracketMatch({match,teams,picks,choose,score,locked,teamIndex}:{teamIndex:Record<string,{name:string;logo:string}>;match:typeof PLAYOFF_MATCHES[number];teams:Array<string|undefined>;picks:Record<string,BracketPick>;choose:(id:string,t:string)=>void;score:(id:string,v:string)=>void;locked:boolean}){const pick=picks[match.id]||{};const options=match.bestOf===7?['4–0','4–1','4–2','4–3']:['3–0','3–1','3–2'];return <article className={`bracketMatch ${pick.winner?'hasPick':''}`}><div className="bracketMatchHead"><span>{match.label}</span><b>BO{match.bestOf}</b></div>{teams.map((code,i)=>{const team=code?teamIndex[code]:undefined;return <button key={i} disabled={!team||locked} className={pick.winner===code?'winner':''} onClick={()=>team&&choose(match.id,code!)}>{team?<><img src={team.logo} alt={`${team.name} logo`}/><span><b>{team.name}</b><small>{pick.winner===code?'ADVANCES':'SELECT WINNER'}</small></span><i>{pick.winner===code?'✓':`#${i+1}`}</i></>:<><span className="tbdLogo">?</span><span><b>TO BE DECIDED</b><small>COMPLETE PREVIOUS SERIES</small></span></>}</button>})}<div className="bracketScore"><label>EXACT SCORE</label><select value={pick.score||''} disabled={!pick.winner||locked} onChange={e=>score(match.id,e.target.value)}><option value="">SELECT</option>{options.map(x=><option key={x}>{x}</option>)}</select></div></article>}

